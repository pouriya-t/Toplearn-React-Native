import React from "react";
import { View, Text } from "react-native";
import Screen from "./../components/shared/Screen";

const CoursesScreen = () => {
  return (
    <Screen style={{alignItems:"center"}}>
      <Text>صفحه دوره ها</Text>
    </Screen>
  );
};

export default CoursesScreen;
