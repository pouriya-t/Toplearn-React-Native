import React from "react";
import { View, Text } from "react-native";
import Screen from "./../components/shared/Screen";

const AccountScreen = () => {
  return (
    <Screen style={{alignItems:"center"}}>
      <Text>صفحه اکانت کاربر</Text>
    </Screen>
  );
};

export default AccountScreen;
